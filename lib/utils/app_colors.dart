import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(
    0xFF2E1A6F,
  ); // color principal (oscuro violeta)
  static const Color background = Color(
    0xFFF6F7FB,
  ); // fondo general (gris muy claro)
  static const Color card = Color(0xFFFFFFFF); // tarjetas en blanco
  static const Color textPrimary = Color(0xFF1F2937); // texto principal
  static const Color textSecondary = Color(0xFF6B7280); // texto secundario
  static const Color border = Color(0xFFE6E9F0); // borde sutil
  static const Color accent = Color(0xFFF7F5FF);
  static const Color success = Color(0xFF16A34A);
  static const Color warning = Color(0xFFEF4444);
  static const Color yellow = Color(0xFFF59E0B);

  // --- AÑADIR ESTOS COLORES ---
  static const Color accentPurple = Color(
    0xFF6B46C1,
  ); // Para el menú activo y botones
  static const Color accentPurpleLight = Color(
    0xFFF3E8FF,
  ); // Para el hover del menú
  // colores para notas prioritarias (rosado claro)
  static const Color priorityLight = Color(0xFFFFF1F2);
  static const Color priority = Color(0xFFFECDD3);

  // Color para la nueva ficha de asesorado
  static const Color navyBlue = Color(0xFF0A192F);
}